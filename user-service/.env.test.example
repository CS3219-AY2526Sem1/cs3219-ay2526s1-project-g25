PORT=3002
BASE_URL=http://localhost:3002
FRONTEND_URL=http://localhost:3000

# Test Supabase (use the same as development for testing)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Test JWT secrets (generate secure random keys for production)
JWT_ACCESS_TOKEN_SECRET=your_test_access_secret_key
JWT_REFRESH_TOKEN_SECRET=your_test_refresh_secret_key

# SMTP (disabled for tests)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=